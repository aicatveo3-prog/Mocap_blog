name: 초안 PR 생성 + 텔레그램 알림

# 루틴이 draft/* 브랜치를 push 하면:
#  1) base(발행) 브랜치로 향하는 PR을 자동 생성하고 (권한 없으면 compare 링크로 대체)
#  2) 텔레그램으로 "검토해주세요" 알림을 보냅니다.
# PR은 사람이 병합해야만 발행됩니다.

on:
  push:
    branches:
      - "draft/**"

permissions:
  contents: read
  pull-requests: write

env:
  BASE_BRANCH: claude/ai-mocap-blog-automation-o273mo

jobs:
  open-pr-and-notify:
    runs-on: ubuntu-latest
    steps:
      - name: 체크아웃
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: PR 생성 시도 (실패 시 compare 링크로 대체)
        id: pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BRANCH="${GITHUB_REF_NAME}"
          URL=$(gh pr list --head "$BRANCH" --state open --json url -q '.[0].url' 2>/dev/null || true)
          if [ -z "$URL" ]; then
            URL=$(gh pr create --base "$BASE_BRANCH" --head "$BRANCH" \
              --title "📝 자동 초안: $BRANCH" \
              --body $'AI가 생성한 초안입니다.\n\n- **병합(Merge)** = 발행 ✅\n- **코멘트** = 수정요청 💬\n- **닫기(Close)** = 폐기 ❌' 2>/dev/null || true)
          fi
          if [ -z "$URL" ]; then
            URL="https://github.com/${GITHUB_REPOSITORY}/compare/${BASE_BRANCH}...${BRANCH}?expand=1"
            echo "⚠️ Actions의 PR 자동생성 권한이 없어 compare 링크로 대체합니다. (Settings에서 켜면 자동 PR 생성됨)"
          fi
          echo "url=$URL" >> "$GITHUB_OUTPUT"

      - name: 새 글 제목 추출
        id: titles
        run: |
          git fetch --no-tags origin "$BASE_BRANCH" || true
          FILES=$(git diff --name-only "origin/${BASE_BRANCH}...HEAD" -- '_posts/*.md' 2>/dev/null || true)
          LIST=""
          for f in $FILES; do
            [ -f "$f" ] || continue
            T=$(grep -m1 '^title:' "$f" | sed -E 's/^title:[[:space:]]*//; s/^"(.*)"$/\1/')
            [ -n "$T" ] && LIST="${LIST}• ${T}"$'\n'
          done
          {
            echo "list<<__EOF__"
            printf '%s' "$LIST"
            echo "__EOF__"
          } >> "$GITHUB_OUTPUT"

      - name: 텔레그램 알림 전송
        if: always()
        env:
          TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          CHAT: ${{ secrets.TELEGRAM_CHAT_ID }}
          URL: ${{ steps.pr.outputs.url }}
          TITLES: ${{ steps.titles.outputs.list }}
        run: |
          if [ -z "$TOKEN" ] || [ -z "$CHAT" ]; then
            echo "TELEGRAM_BOT_TOKEN / TELEGRAM_CHAT_ID 시크릿이 없습니다."; exit 1
          fi
          [ -n "$URL" ] || URL="https://github.com/${GITHUB_REPOSITORY}/branches"
          TEXT=$(printf '📝 오늘의 AI 모션캡쳐 초안이 준비됐어요.\n\n%s\n검토·승인 👉 %s\n(병합=발행 · 코멘트=수정 · 닫기=폐기)' "$TITLES" "$URL")
          RESP=$(curl -sS -X POST "https://api.telegram.org/bot${TOKEN}/sendMessage" \
            --data-urlencode "chat_id=${CHAT}" \
            --data-urlencode "text=${TEXT}" \
            -d "disable_web_page_preview=false")
          echo "$RESP" | grep -q '"ok":true' && echo "텔레그램 전송 성공" || { echo "텔레그램 전송 실패: $RESP"; exit 1; }
