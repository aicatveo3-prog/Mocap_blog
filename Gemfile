source "https://rubygems.org"

# GitHub Pages 는 저장소를 자동으로 Jekyll 로 빌드합니다.
# 로컬에서 미리보기하려면:  bundle install && bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
